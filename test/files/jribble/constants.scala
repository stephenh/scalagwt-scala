class Constants {
  def chars = {
    val minChar = Char.MinValue
    val maxChar = Char.MaxValue
    //should be translated into \t
    val tabChar = '\011'
  }
  
  def strings = {
    val string = "\011"
  }
}
